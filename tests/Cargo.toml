[package]
name = "braidpool"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]  # Dependencies only used for tests
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
